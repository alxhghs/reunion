import React, { useState } from "react";
import Head from "next/head";

const Home = () => {
    const [data, setData] = useState([]);

    return (
        <Head>
            <title>Reunion</title>
        </Head>
    )
};

export default Home;
